<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!-- Seed roles -->
    <changeSet id="005-1" author="finvault">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">SELECT COUNT(*) FROM roles WHERE name = 'ROLE_ADMIN'</sqlCheck>
        </preConditions>
        <insert tableName="roles">
            <column name="name" value="ROLE_ADMIN"/>
        </insert>
        <rollback>
            <delete tableName="roles">
                <where>name = 'ROLE_ADMIN'</where>
            </delete>
        </rollback>
    </changeSet>

    <changeSet id="005-1b" author="finvault">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">SELECT COUNT(*) FROM roles WHERE name = 'ROLE_ANALYST'</sqlCheck>
        </preConditions>
        <insert tableName="roles">
            <column name="name" value="ROLE_ANALYST"/>
        </insert>
        <rollback>
            <delete tableName="roles">
                <where>name = 'ROLE_ANALYST'</where>
            </delete>
        </rollback>
    </changeSet>

    <changeSet id="005-1c" author="finvault">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">SELECT COUNT(*) FROM roles WHERE name = 'ROLE_VIEWER'</sqlCheck>
        </preConditions>
        <insert tableName="roles">
            <column name="name" value="ROLE_VIEWER"/>
        </insert>
        <rollback>
            <delete tableName="roles">
                <where>name = 'ROLE_VIEWER'</where>
            </delete>
        </rollback>
    </changeSet>

    <!-- Seed admin user: admin / admin123 -->
    <changeSet id="005-2" author="finvault">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="0">SELECT COUNT(*) FROM users WHERE username = 'admin' OR email = 'admin@finvault.com'</sqlCheck>
        </preConditions>
        <insert tableName="users">
            <column name="username" value="admin"/>
            <column name="email" value="admin@finvault.com"/>
            <!-- BCrypt hash of 'admin123' -->
            <column name="password" value="$2a$10$XVXZsa1.Rb2aZcbHy2tlaeS6VsvZneu9Fh4N/OnMsIvP.1WaZTdri"/>
            <column name="full_name" value="System Administrator"/>
            <column name="status" value="ACTIVE"/>
            <column name="created_at" valueComputed="CURRENT_TIMESTAMP"/>
            <column name="updated_at" valueComputed="CURRENT_TIMESTAMP"/>
        </insert>
        <rollback>
            <delete tableName="users">
                <where>username = 'admin'</where>
            </delete>
        </rollback>
    </changeSet>

    <!-- Assign ADMIN role to admin user -->
    <changeSet id="005-3" author="finvault">
        <preConditions onFail="MARK_RAN">
            <!-- Ensure both user and role exist before attempting connection -->
            <sqlCheck expectedResult="1">SELECT COUNT(*) FROM users WHERE username = 'admin'</sqlCheck>
            <sqlCheck expectedResult="1">SELECT COUNT(*) FROM roles WHERE name = 'ROLE_ADMIN'</sqlCheck>
            <!-- Ensure mapping doesn't exist already -->
            <sqlCheck expectedResult="0">
                SELECT COUNT(*) FROM user_roles ur
                JOIN users u ON ur.user_id = u.id
                JOIN roles r ON ur.role_id = r.id
                WHERE u.username = 'admin' AND r.name = 'ROLE_ADMIN'
            </sqlCheck>
        </preConditions>
        <sql>
            INSERT INTO user_roles (user_id, role_id)
            SELECT u.id, r.id FROM users u, roles r
            WHERE u.username = 'admin' AND r.name = 'ROLE_ADMIN'
        </sql>
        <rollback>
            <sql>
                DELETE FROM user_roles 
                WHERE user_id = (SELECT id FROM users WHERE username = 'admin')
                  AND role_id = (SELECT id FROM roles WHERE name = 'ROLE_ADMIN')
            </sql>
        </rollback>
    </changeSet>

</databaseChangeLog>
