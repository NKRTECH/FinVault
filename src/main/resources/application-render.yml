spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

# Render exposes a single port — merge actuator onto the main server port
management:
  server:
    port: ${PORT:8080}

server:
  port: ${PORT:8080}
